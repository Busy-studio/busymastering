# Busy Auto Mastering

Upload a WAV file, start mastering, and download the mastered WAV when processing is complete.

## Streamlit Secrets

```toml
SERVICE_ENDPOINT_URL = "https://your-service-url"
SERVICE_ACCESS_TOKEN = "your-access-token"
SERVICE_TIMEOUT_SEC = "60"
SERVICE_START_TIMEOUT_SEC = "3600"
```
